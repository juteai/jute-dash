# One Dark

One Dark is a familiar cool-toned editor theme adapted for dashboard surfaces. Light mode uses a One Light-style companion palette so `color-mode` can still work normally.

Supported modes: light and dark.
